# .agents/AGENTS.md — mymind project notes

This repo is a Printing Press generated Go CLI and MCP server. The old npm/TypeScript `@nawwal/mymind` package layout is obsolete.

Use the root [AGENTS.md](../AGENTS.md) as the active implementation guide.

Important local artifacts:

- `mymind-openapi-spec.md`: human-readable OpenAPI/spec research notes.
- `generated/mymind.openapi.yaml`: machine-readable OpenAPI input used by Printing Press.
- `skills/release/SKILL.md`: release skill, if present.

Do not restore old `.agents/api-context.md`, `.agents/mcp-surface.md`, npm schema docs, or TypeScript workflow notes unless the project intentionally returns to that architecture.
